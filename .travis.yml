#arch: ppc64le
os: linux
dist: xenial
before_script:
     - sudo apt-get update -y
language: node_js
node_js:
     - 12 
install:
     - npm install -g grunt-cli bower
     - npm install
     - bower install
script:
     - npm test
