#arch: ppc64le
os: linux
dist: xenial
before_script:
     - nvm use
     - npm install --global yarn
     - yarn kbn bootstrap
     - npm install -g node-gyp
     - yarn kbn bootstrap
language: node_js
#node_js:
#     - 12 
#install:
#     - npm install -g grunt-cli bower
#     - npm install
#     - bower install
#script:
 #    - npm test
